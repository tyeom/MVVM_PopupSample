<UserControl x:Class="MVVM_PopupSample.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_PopupSample.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Content="Show Confirm MessageBox"
                    Command="{Binding ShowConfirmMsg}"/>
            <Button Content="Show Confirm2 MessageBox"
                    Command="{Binding ShowConfirmMsg2}"/>
            <Button Content="Show YesNo MessageBox"
                    Command="{Binding ShowYesNoMsg}"/>
            <Button Content="Show Popup Window1"
                    Command="{Binding ShowPopup1}"/>
            <Button Content="Show Popup Window2"
                    Command="{Binding ShowPopup2}"/>
        </StackPanel>
    </Grid>
</UserControl>
